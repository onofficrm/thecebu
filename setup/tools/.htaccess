# Web access disabled — use CLI only (e.g. php setup/tools/eottae-install-cli.php)
<IfModule mod_authz_core.c>
    Require all denied
</IfModule>
<IfModule !mod_authz_core.c>
    Order deny,allow
    Deny from all
</IfModule>
